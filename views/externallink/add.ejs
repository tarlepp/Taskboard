<form id="formExternalLinkNew" class="form-horizontal">
    <input type="hidden" name="_csrf" value="<%= _csrf %>" />
    <input type="hidden" name="projectId" value="<%= projectId %>" />

    <div class="form-group required">
        <label class="col-lg-3 control-label" for="formExternalLinkNewTitle">Title</label>
        <div class="col-lg-9">
            <input id="formTaskNewTitle" name="title" type="text" placeholder="enter external link title" class="form-control" required="required" />
        </div>
    </div>

    <div class="form-group required">
        <label class="col-lg-3 control-label" for="formTaskNewLink">External link</label>
        <div class="col-lg-9">
            <input id="formTaskNewLink" name="link" type="text" placeholder="enter base of external link" class="form-control" required="required"
                data-validate-type="url"
            />
        </div>
    </div>

    <div class="form-group">
        <label class="col-lg-3 control-label" for="formTaskNewLink">Dynamic parameter(s)</label>
        <div class="col-lg-9">
            <p id="decodedUrl" class="form-control-static"
                data-text-original="<em>Please enter external link</em>"
                data-text-no-parameters="<em>Given URL doesn't contain any parameters</em>"
            >
                <em>Please enter external link</em>
            </p>
        </div>

        <div class="col-lg-offset-3 col-lg-9">
            <span class=" help-block">
                You can specify <code class="text-small">dynamic parameters</code> for the external link. Every dynamic
                parameter must be defined in link as <code class="text-small">:param</code>, where param is shown in
                external link add GUI as in input field for current external link. Example link can be as in following
                format: <code class="text-small">http://www.foobar.com/?foo=:foo&bar=:bar</code> This external link will
                have two dynamic parameters that user has to specify when he / she is attaching this link to any object.
            </span>
        </div>
    </div>

    <div class="form-group">
        <label class="col-lg-3 control-label" for="formExternalLinkNewDescription">Description</label>
        <div class="col-lg-9">
            <textarea id="formTaskNewDescription" name="description" class="form-control" data-wysiwyg="true" placeholder="enter external link description"></textarea>
        </div>
    </div>
</form>