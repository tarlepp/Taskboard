'use strict';

var _ = require('lodash');

/**
 * EpicController
 *
 * @description :: Server-side logic for managing Epics
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */
module.exports = _.merge(_.cloneDeep(require('../base/Controller')), {
});
