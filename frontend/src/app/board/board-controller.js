(function() {
    'use strict';

    angular.module('Taskboard.controllers')
        .controller('BoardController',
            [
                '$scope', '$state', 'Auth',
                function($scope, $state, Auth) {
                }
            ]
        );
}());